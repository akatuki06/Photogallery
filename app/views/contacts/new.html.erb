<%= form_for @contact do |f| %>

<div>
	<%= f.label :name %>
	<%= f.text_field :name %>
</div>

<div>
	<%= f.label :email %>
	<%= f.text_field :email %>
</div>

<div>
	<%= f.label :message %>
	<%= f.text_area :message %>
</div>

<div>
	<%= f.submit "送信" %>
</div>

<% end %>
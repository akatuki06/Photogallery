        <div class="col-lg-8 m-3">
          <nav class="navbar navbar-expand-md navbar-dark bg-primary">
            <div class="container">
              <a class="navbar-brand" href="#">
                <b>Sales</b>
              </a>
              <div class="collapse navbar-collapse text-center justify-content-end" id="navbarPrimarySupportedContent"> </div>
            </div>
          </nav>
          <h2 class="my-3">検索フォーム</h2>
          <hr>
          <form class="">
            <div class="form-group">
              <label>登録期間</label>
		          <%= search_form_for @search, url: admin_sales_index_path do |f| %>
		          <%= f.date_field :created_at_gteq %> 
		          ~
		          <%= f.date_field :created_at_lteq_end_of_day %>
		          <div class="form-group">
                <label>ID</label>
                <div>
                <%= f.search_field :id_eq, size: "70x10", class: "form-control" %>
              </div>
            </div>
              <div class="form-group">
                <label>タイトル</label>
                <div>
                <%= f.search_field :title_cont, size: "70x10", class: "form-control" %>
                </div>
              </div>
              <div class="form-group">
                <label>アーティスト名</label>
                <div>
                <%= f.search_field :artist_name_cont, size: "70x10", class: "form-control" %>
                </div>
              </div>
		          <%= f.submit "検索", class: "btn btn-primary" %>
		          <% end %>
              <hr> </div>
            </form>
          </div>


          <div class="container">
            <div class="row">
              <div class="col-lg-12 justify-content-center">
                <h2 class="my-3">検索結果</h2>
                <hr>
                <table class="table">
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>タイトル</th>
                      <th>アーティスト名</th>
                      <th>作成日時</th>
                      <th></th>
                    </tr>
                  </thead>
                  <% @exhibitions.each do |e| %>
                  <tbody>
                    <tr>
                      <td><%= e.id %></td>
                      <td><%= e.title %></td>
                      <td><%= e.artist.name %></td>
                      <td><%= e.created_at.strftime('%Y年%m月%d日 %H:%M') %></td>
                      <td>
                        <%= link_to admin_exhibition_path(e.id), method: :delete, data:{conform:'削除しますか？'} do  %>
                        <button type="submit" class="btn btn-danger btn-sm mx-2">削除</button>
                      </td>
                      <% end %>
                      </td>
                    </tr>
                  </tbody>
                  <% end %>
                </table>
              </form>
            </div>
          </div>
        </div>
        <div class="py-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
         <div class="row", style="margin-bottom: 60px">
          <%= paginate @exhibitions, class: "pagenate"%>
        </div>
        </div>
      </div>
    </div>
  </div>
      </div>
    </div>
  </div>